\documentclass[12pt,a4paper]{report}

% ── Encodage et langue ──
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
% babel french non disponible dans cet environnement
% Pour compiler localement, décommenter :
% \usepackage[french]{babel}

% ── Mise en page ──
\usepackage[margin=2.5cm, top=3cm, bottom=3cm]{geometry}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{setspace}
\onehalfspacing

% ── Mathématiques ──
\usepackage{amsmath, amssymb, amsthm, mathtools}

% ═══════════════════════════════════════
%   POLICES
%   Corps : Palatino (newpx) · Titres : Fira Sans · Code : Inconsolata
% ═══════════════════════════════════════
\let\Bbbk\relax                       % évite le conflit amssymb / newpxmath
\usepackage[scaled=.98]{newpxtext}
\usepackage[varg,bigdelims]{newpxmath}
\usepackage[scaled=0.92]{FiraSans}    % \sffamily (titres, encadrés)
\usepackage[varqu,scaled=0.86]{zi4}   % \ttfamily (code, Inconsolata)
\usepackage[activate={true,nocompatibility},final,tracking=true,factor=1100]{microtype}

% ── Graphiques et couleurs ──
\usepackage{tikz}
\usetikzlibrary{arrows.meta, calc, positioning, decorations.pathreplacing, matrix}
\usepackage[most]{tcolorbox}
\usepackage{xcolor}

% ── Divers ──
\usepackage{enumitem}
\setlist{itemsep=2pt, topsep=4pt, parsep=0pt}
\usepackage{hyperref}
\usepackage{bookmark}

% ═══════════════════════════════════════
%   PALETTE DE COULEURS
% ═══════════════════════════════════════
\definecolor{deepblue}{HTML}{1B3A5C}
\definecolor{accentblue}{HTML}{2E86C1}
\definecolor{lightblue}{HTML}{D6EAF8}
\definecolor{deepgreen}{HTML}{1E6F50}
\definecolor{lightgreen}{HTML}{D5F5E3}
\definecolor{deepred}{HTML}{922B21}
\definecolor{lightred}{HTML}{FADBD8}
\definecolor{deeporange}{HTML}{B9770E}
\definecolor{lightorange}{HTML}{FDEBD0}
\definecolor{deeppurple}{HTML}{6C3483}
\definecolor{lightpurple}{HTML}{EBDEF0}
\definecolor{darkgray}{HTML}{2C3E50}
\definecolor{softgray}{HTML}{7F8C9B}
\definecolor{pagebg}{HTML}{FDFEFE}

% ═══════════════════════════════════════
%   ENVIRONNEMENTS THÉORIQUES
% ═══════════════════════════════════════
\theoremstyle{definition}
\newtheorem{definition}{Définition}[chapter]
\newtheorem{exemple}{Exemple}[chapter]

\theoremstyle{plain}
\newtheorem{theoreme}{Théorème}[chapter]
\newtheorem{proposition}{Proposition}[chapter]
\newtheorem{corollaire}{Corollaire}[chapter]
\newtheorem{lemme}{Lemme}[chapter]

\theoremstyle{remark}
\newtheorem{remarque}{Remarque}[chapter]

% ═══════════════════════════════════════
%   BOÎTES COLORÉES (tcolorbox)
% ═══════════════════════════════════════

% Style de base commun à tous les encadrés
\tcbset{
  rnbase/.style={
    enhanced, breakable,
    fonttitle=\bfseries\sffamily,
    fontupper=\normalfont,
    coltitle=white,
    arc=4pt, boxrule=0.9pt,
    left=10pt, right=10pt, top=7pt, bottom=7pt,
    boxsep=1pt,
    toptitle=1.5pt, bottomtitle=1.5pt,
    enlarge top by=1pt, enlarge bottom by=1pt,
  }
}

% Boîte Définition
\newtcolorbox{defbox}[1][]{
  rnbase,
  colback=lightblue!55, colframe=accentblue,
  title={Définition~#1}
}

% Boîte Théorème
\newtcolorbox{thmbox}[1][]{
  rnbase,
  colback=lightgreen!70, colframe=deepgreen,
  title={Théorème~#1}
}

% Boîte Proposition
\newtcolorbox{propbox}[1][]{
  rnbase,
  colback=lightgreen!40, colframe=deepgreen!75,
  title={Proposition~#1}
}

% Boîte Preuve / Démonstration (sobre, barre latérale)
\newtcolorbox{proofbox}[1][Démonstration]{
  enhanced, breakable,
  colback=white, colframe=softgray!45,
  coltitle=darkgray, fonttitle=\itshape\sffamily,
  title={#1},
  arc=2pt, boxrule=0.5pt,
  left=12pt, right=10pt, top=6pt, bottom=6pt,
  borderline west={2.5pt}{0pt}{softgray!55},
}

% Boîte Remarque
\newtcolorbox{rembox}[1][Remarque]{
  rnbase,
  colback=lightorange!70, colframe=deeporange,
  title={#1}
}

% Boîte Exemple
\newtcolorbox{exbox}[1][Exemple]{
  rnbase,
  colback=lightpurple!45, colframe=deeppurple!75,
  title={#1}
}

% Boîte Intuition personnelle (mise en avant)
\newtcolorbox{intuitionbox}[1][Mon intuition]{
  rnbase,
  colback=lightred!35, colframe=deepred!75,
  boxrule=1.3pt,
  title={\(\star\)~#1},
  drop fuzzy shadow=deepred!25,
}

% Boîte Méthode (mise en avant)
\newtcolorbox{methodbox}[1][Méthode]{
  rnbase,
  colback=white, colframe=accentblue!85,
  boxrule=1.3pt,
  title={#1},
  drop fuzzy shadow=accentblue!25,
}

% ═══════════════════════════════════════
%   EN-TÊTES ET PIEDS DE PAGE
% ═══════════════════════════════════════
\setlength{\headheight}{15pt}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\sffamily\color{softgray}\nouppercase{\leftmark}}
\fancyhead[R]{\small\sffamily\color{softgray}Cahier de Recherche}
\fancyfoot[C]{\sffamily\color{softgray}\thepage}
\renewcommand{\headrulewidth}{0.6pt}
\renewcommand{\footrulewidth}{0pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{accentblue!55}\leaders\hrule height \headrulewidth\hfill}}

% ═══════════════════════════════════════
%   FORMAT DES TITRES
% ═══════════════════════════════════════
% Noms français (babel désactivé dans cet environnement)
\renewcommand{\chaptername}{Chapitre}
\renewcommand{\appendixname}{Annexe}

% Chapitre : numéro accentué + filet coloré
\titleformat{\chapter}[display]
  {\normalfont\sffamily}
  {\raggedright\bfseries\large\color{accentblue}%
    \MakeUppercase{\chaptertitlename}\hspace{0.4em}%
    {\fontsize{40}{40}\selectfont\color{deepblue}\thechapter}}
  {6pt}
  {\Huge\bfseries\color{deepblue}}
  [\vspace{6pt}{\color{accentblue!70}\titlerule[2pt]}]
\titlespacing*{\chapter}{0pt}{4pt}{26pt}

% Section : numéro accentué + filet fin clair dessous
\titleformat{\section}
  {\normalfont\Large\bfseries\sffamily\color{deepblue}}
  {\color{accentblue}\thesection}{0.8em}{}
  [\vspace{2pt}{\color{lightblue}\titlerule[1.2pt]}]

% Sous-section
\titleformat{\subsection}
  {\normalfont\large\bfseries\sffamily\color{accentblue}}
  {\color{accentblue!70}\thesubsection}{0.7em}{}

% ═══════════════════════════════════════
%   COMMANDES PERSONNALISÉES
% ═══════════════════════════════════════
\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\Q}{\mathbb{Q}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\F}{\mathbb{F}}
\newcommand{\K}{\mathbb{K}}
\DeclareMathOperator{\re}{Re}
\DeclareMathOperator{\im}{Im}
\DeclareMathOperator{\argop}{arg}
\DeclareMathOperator{\Ker}{Ker}
\DeclareMathOperator{\Ima}{Im}
\newcommand{\abs}[1]{\left|#1\right|}

% ═══════════════════════════════════════
%   HYPERREF CONFIG
% ═══════════════════════════════════════
\hypersetup{
  colorlinks=true,
  linkcolor=deepblue,
  urlcolor=accentblue,
  citecolor=deepgreen,
  pdftitle={Cahier de Recherche -- Mathématiques},
  pdfauthor={},
  bookmarksnumbered=true,
}
